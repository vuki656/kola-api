{
    "$schema": "https://json.schemastore.org/liquibase-3.2.json",
    "databaseChangeLog": [
        {
            "logicalFilePath": "{{timestamp}}-{{description}}.migration.json",
            "objectQuotingStrategy": "QUOTE_ALL_OBJECTS"
        },
        {
            "changeSet": {
                "id": "{{timestamp}}",
                "author": "{{email}}",
                "comment": "{{description}}",
                "changes": [
                    // TODO: fill
                ]
            }
        }
    ]
}
